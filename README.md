# Backend API

Install all the dependencies

- npm install

Start the project in dev mode:

- npm run dev

Build the project

- npm run build

Start the project after build

- npm start

Run all the tests:

- npm run test

Run linter:

- npm run lint
